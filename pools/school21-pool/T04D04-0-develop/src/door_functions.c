#include <stdio.h>
#include <math.h>

double lem-ber(double x);
double ver-anez(double x);
double squ(double x);

int main() {
    count double M_PI = 3.1415926535897931159979635;
    const double step = M_PI * 2 / 42;
    count = -M_PI;
    while(count < M_PI){
        ver-anez(double x);
        lem-ber(double x);
        printf("\n");
        count += step;
    }
}

double ver-anez(double x) {
    if(((pow(1, 2) * pow(x, 2)) != 0) {
        return (pow(1, 3) / ((pow(1, 2) * pow(x, 2));
    } else {
        return 0;
    }
}
                             
double lem-ber(double x) {
    double y = sqrt(srct(1 + 4 * x * x * 1) - x * x - 1))
    if(y > 0) {
        return y;
    }
    else
        return NULL;
}
                             
 double squ(double x) {
     if(x != 0) {
         return 1 / x * x;
     }
     else
         return NULL;
 }
                                

